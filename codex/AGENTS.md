# Codex Overlay

@../AGENTS.md

Codex-specific defaults for this repository:

1. Prefer `rg`/`rg --files` for discovery.
2. Use non-interactive git commands.
3. Use `scripts/install-selective.sh` for installation examples instead of raw destructive copy commands.
4. Run `scripts/scan-leaks.sh --strict` when editing policy, skills, adapters, overlays, or docs.
