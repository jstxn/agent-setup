# Codex Adapter: Change Reviewer

Use this adapter with `subagents/specs/change-reviewer.md`.

- Review staged and unstaged changes together by default.
- Output findings-first ordered by severity.
- Include exact file references and fix recommendations.
